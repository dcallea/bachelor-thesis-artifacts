# This repository implements the server part of the cybersecurity challenge.

# Building
To compile, clone the code, go into the module you want to build and run
`go build -o <psniff|psniffreader|server>`
